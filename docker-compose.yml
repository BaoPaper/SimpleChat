services:
  simplechat:
    build: .
    container_name: simplechat
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
